# Firestore schema

## Collection `cars`

The document id is the car id; the app reads it via `doc.id`, so no `id` field is needed inside the document.

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| `model` | string | yes | Display name. Falls back to "Unknown model". |
| `distance` | number | no | Range on a full tank or charge, in km. Defaults to 0. |
| `fuelCapacity` | number | no | Litres, or kWh when `fuelType` is `electric`. Defaults to 0. |
| `pricePerDay` | number | no | Daily rate. Defaults to 0. |
| `pricePerHour` | number | no | Legacy. Read only when `pricePerDay` is absent, so old documents keep working. |
| `imageUrl` | string | no | Remote photo. Falls back to the bundled asset when absent or broken. |
| `location` | geopoint | no | Pick-up point. `latitude` / `longitude` number fields are accepted as an alternative. |
| `seats` | number | no | Hidden from the specs row when 0. |
| `transmission` | string | no | `automatic` (or `auto`) / `manual`. Anything else reads as unknown and is hidden. |
| `fuelType` | string | no | `petrol` (or `gasoline`, `gas`) / `diesel` / `hybrid` / `electric` (or `ev`). |
| `rating` | number | no | 0 to 5. The badge is hidden when 0. |
| `reviewCount` | number | no | |
| `available` | boolean | no | Defaults to `true`. `false` disables booking and shows a "Booked" badge. |

### Parsing guarantees

- Numbers accept `int`, `double` and numeric strings. Firestore stores whole numbers as `int`, which used to crash the list load.
- Every field except `model` has a default, so adding fields never breaks existing documents.
- A document that fails to parse entirely is skipped rather than failing the whole query.

### Seeding

```bash
flutter run -t tool/seed_firestore.dart -d chrome
```

Writes five sample cars. Documents with matching ids are overwritten; everything else is left alone.

## Collection `bookings`

Written by the booking flow. The document id is generated by Firestore, and the
user-facing reference code (`CR-XXXXXX`) is derived from it.

| Field | Type | Notes |
|-------|------|-------|
| `carId` | string | Document id from `cars`. |
| `carModel` | string | Denormalised so the bookings list needs no join. |
| `userId` | string | Firebase Auth uid, anonymous by default. |
| `start` | timestamp | Pick-up date, stored at midnight. |
| `end` | timestamp | Return date, stored at midnight. |
| `totalCents` | number | Rental total in cents, deposit excluded. |
| `depositCents` | number | Refundable deposit in cents. |
| `status` | string | `pending` / `confirmed` / `active` / `completed` / `cancelled`. Unknown values read as `pending`. |
| `createdAt` | timestamp | |

### Money

Amounts are held in whole cents throughout. Doing the arithmetic in `double`
dollars lets rounding drift, so a displayed total can disagree with the sum of
the lines above it.

### Availability

Before writing, the app queries bookings for the car with `end` after the
requested start, then checks the `start` side in memory: Firestore permits a
range filter on only one field. A booking is treated as blocking unless it is
`cancelled` or `completed`. Touching ends do not clash, so one rental may end on
the day the next begins.

**Known limitation:** the availability check and the write are two separate
operations, so two users confirming the same dates at the same instant could
both succeed. Closing that needs a server-side transaction — a Cloud Function or
a security rule. The current check covers the case that actually occurs: a user
picking dates another user already took.

### Suggested indexes

Both queries are single-field and covered by the automatic indexes:

- `carId` equality plus `end` range
- `userId` equality (results are sorted client-side, so no composite index)

### Suggested security rules

```
match /bookings/{id} {
  allow read: if request.auth != null && resource.data.userId == request.auth.uid;
  allow create: if request.auth != null
                && request.resource.data.userId == request.auth.uid;
  allow update: if request.auth != null && resource.data.userId == request.auth.uid;
  allow delete: if false;
}
```

Anonymous sign-in must be enabled in the Firebase console for booking to work.
